from setuptools import setup

# This setup.py is maintained for backward compatibility and editable installs.
# All package metadata and dependencies are now defined in pyproject.toml
# which follows PEP 621 standards for modern Python packaging.

setup()
